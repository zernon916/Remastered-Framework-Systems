-- RfsCorn.lua — UNUSED (parked). Corn stacks use shapeset itemStack + stackedAmount.
-- Kept so old worlds / experiments do not break if something still dofiles this path.
-- Do not attach via shapeset unless explicitly re-enabling scripted qty.

RfsCorn = class()
